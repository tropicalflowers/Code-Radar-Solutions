#include <stdio.h>
int main()
{
    int a,b;
    scanf("%d", &a);
    for(int i; i<=a; i++)
    {
        while(b=1; b<=i; b++)
        {

            printf("%d", b);
        }
        printf("\n");
    }
}