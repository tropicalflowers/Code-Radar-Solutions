#include <stdio.h>
int main()
{
    int a,b;
    scanf("%d", &a);
    for(int i; i<=a; i++)
    {
        while(b<=i)
        {

            printf("%d", b);
        }
        printf("\n");
    }
}